#include "aescrypt/app.h"
#include "aescrypt/aes.h"
#include "aescrypt/file.h"
#include "aescrypt/utils.h"

#include <string>

using std::string;

void AESCryptApp::encrypt_file(string& in_path, string& out_path, string& password) {
    // 1) 
}

void AESCryptApp::decrypt_file(string& in_path, string& out_path, string& password) {
    // empty for now
}